# ZENVORA — CURRENT STATE MATRIX (launch-ops audit, 2026-09-12)

Status vocabulary (mandated): VERIFIED · PASS · BLOCKED · OWNER ACTION REQUIRED · DEFECT FOUND · RISK · NOT TESTABLE.
Every row cites evidence actually observed this audit — no prior report was trusted.

| # | AREA | STATUS | EVIDENCE | DEFECT | ACTION |
|---|------|--------|----------|--------|--------|
| 1 | Release identity (code = deployed = intended) | VERIFIED | `git rev-parse HEAD^{tree}` = `origin/main^{tree}` = `15e71b07cdc87e17bd3ef8c5ce44adc8fae6df87`; `git diff HEAD origin/main` empty; origin/main `21a81e3 "Release v10…"` (owner-squashed history) | — | none |
| 2 | Production version = v10 | VERIFIED | Live markers: home `rel=canonical` (added `b24ac3f`); checkout chunk `page-14a784d1c30a3427.js` contains `line2:A.line2.trim()\|\|void 0` (D1 fix); 404 page has SVG `<text>` watermark (F6) | — | none |
| 3 | APP_URL / canonical host | DEFECT FOUND → OWNER ACTION REQUIRED | Live canonical + `og:url` + every `sitemap.xml` `<loc>` + robots `Host:`/`Sitemap:` = `https://zenvora-store.vercel.app`, which returns **HTTP 402** (dead host). Real host: `https://zenvorastore.vercel.app`. Root cause: stale instruction in `USER_INPUT_REQUIRED.md` B3 (fixed in repo this audit) | Wrong env value in Vercel | Owner: Vercel → Settings → Env → `APP_URL=https://zenvorastore.vercel.app` (Production+Preview) → redeploy; then re-verify canonical/sitemap host |
| 4 | Production database | VERIFIED (structure) / empty catalogue by design | `/api/health` `database:true`; `prisma migrate deploy` runs in build chain (build succeeded ⇒ migrations applied); QA user register 201 + login 200 across separate requests ⇒ Neon read/write proven; shop/search render honest "No products found"; PDP/category URLs 404 (no rows) | — | Populate with REAL products via admin (never demo seed — mandate rule 21) |
| 5 | Admin bootstrap mechanism | VERIFIED (code) / NOT TESTABLE (prod record) | `scripts/create-admin.ts` runs inside `npm run build`; idempotent; never invents credentials; skips with `[create-admin] … not set — skipping` warning; local build log shows the `already exists (role: ADMIN) — left untouched` path works | Unknown whether ADMIN_EMAIL/ADMIN_PASSWORD were set when prod build ran | Owner: Vercel → Deployments → current → Build logs → search `[create-admin]`. If "not set": add both env vars (unique strong password — never in chat) → redeploy → log shows "created admin user" → login at `/auth/login` |
| 6 | Auth / session / RBAC on production | PASS | Live: register 201; login 200 (`role:CUSTOMER`); `/api/auth/me` = 200 `{user:…}` → after logout cookie removed from jar + `{user:null}` (matches `auth-flow.test.ts:88-91` design: me is intentionally non-throwing); non-staff GET `/api/admin/inventory/movements` → **403 JSON "Administrator access required."** (no data); unauth `/admin` → 307 → `/auth/login?next=/admin` | — | none |
| 7 | Earlier probe "anomalies" | NOT DEFECTS (probe errors) | `/api/admin/products` exports **POST only** ⇒ GET→405 is correct Next.js routing; me-after-logout 200 is designed behavior (row 6) | — | none |
| 8 | Fake-payment risk (`/api/payments/test/simulate`) | PASS | Double gate `isTestPaymentsAllowed() = PAYMENTS_TEST_MODE && !isProduction` (env.ts:117-119) → prod 404; live `/api/health` says `environment:"production", paymentsTestMode:false`; live probe blocked at CSRF layer (403) before handler | — | none |
| 9 | Demo supplier in production | PASS (safe) | `isDemoSupplierAllowed() = SUPPLIER_DEMO_MODE && !isProduction` (env.ts:122-123) ⇒ no fake fulfilment possible on prod regardless of env flag | — | none |
| 10 | Cron / background jobs | DEFECT FOUND (ops gap) → FIXED in repo, awaiting deploy | `CRON_SECRET` IS set on prod (unauth/wrong-token probes → 401 "Invalid cron secret", not 503); timing-safe compare (safeEqual). BUT no scheduler existed: no `vercel.json`, route was POST-only (Vercel Cron sends GET) ⇒ jobs ran only via in-process kicks | No reliable periodic runner | Repo now has: cron route GET alias (same guard) + `vercel.json` daily backstop (zero owner effort — Vercel sends Bearer CRON_SECRET automatically) + `.github/workflows/cron-jobs.yml` (*/5 min, optional; owner adds `CRON_SECRET` as GitHub repo secret to activate) |
| 11 | Payments (Razorpay) | OWNER ACTION REQUIRED (later phase) | `/api/health`: `paymentsConfigured:false, paymentsProvider:"NONE"` — honest, COD-only until configured | — | Phase: owner Razorpay account/KYC → test keys first (never paste in chat) |
| 12 | Storage / product images | RISK | `STORAGE_PROVIDER` enum local\|s3\|cloudinary, default `local` (env.ts:42); local disk is **ephemeral on Vercel** ⇒ uploaded product images would not persist across invocations/deploys | Prod uploads unsafe until provider configured | Phase 7/8: choose Cloudinary or S3-compatible; owner creates account + keys; verify upload→persist→serve |
| 13 | Email | NOT TESTABLE externally / OWNER ACTION REQUIRED (later) | Provider abstraction exists; prod provider value not observable without secrets; order emails unverifiable until catalogue+order exist | — | Phase 6/17: SMTP config by owner; then test email |
| 14 | Security headers (prod) | PASS | Live headers: CSP (self + Razorpay allowlist, object-src none, frame-ancestors self, upgrade-insecure-requests), HSTS 2y includeSubDomains, X-Content-Type-Options nosniff, X-Frame-Options SAMEORIGIN, Referrer-Policy, Permissions-Policy (camera/mic/geo/topics off) | — | none |
| 15 | Mobile QA on production | PASS | Puppeteer (Chrome 148, Android UA) live sweep 9 routes × {360×800, 390×844} = 18 pages: 0 horizontal overflow, 0 page errors, expected statuses (incl. true 404); screenshots `/home/user/qa/shots-live/` | — | Re-run deeper matrix once catalogue exists |
| 16 | SEO served output (structure) | PASS except host defect | title/desc/OG/twitter present; JSON-LD Organization+WebSite+SearchAction on home; auth pages noindex ✓; `/xyz-nope` true 404 ✓; robots disallows /api,/account,/admin,/checkout,/auth,/order | All absolute URLs point at dead host (row 3) | Fixed by APP_URL correction |
| 17 | Live checkout journey (behavioral) | BLOCKED (no catalogue) | Cannot add-to-cart with zero products. D1 line-2 fix verified statically in live chunk (row 2); full behavioral journey requires ≥1 real product | — | After first real product: complete Phase 11 journey + one controlled COD order |
| 18 | Local verification battery (after cron change) | PASS | `tsc` 0 errors (TS 5.9.3) · ESLint clean · **194/194** unit+integration (22 files) · `npm run build` ✓ 24.3s · shared First Load JS **103 kB** unchanged · E2E re-run scheduled with Phase 13 (cron change is additive; no test covers cron directly) | — | Full E2E before next deploy recommendation |
| 19 | Rate limiting | RISK (documented, unchanged) | In-memory per-instance limiter; login lockout proven by E2E locally; on Vercel multi-instance the limit is per-instance, not global | Not distributed | Keep documented; consider Upstash/Redis only if abuse observed (do not add fashionable deps pre-emptively) |
| 20 | Repo push access from sandbox | NOT TESTABLE/BLOCKED | Remote added (`https://github.com/Manashjyoti-Bora/zenvora-store.git`); fetch works (public read); push credentials absent by design (never in chat) | — | Launch-ops delta delivered as zip; owner commits/pushes (their established flow) |
